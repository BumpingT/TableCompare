# 📋 表格对比工具 TableDiff — 项目总结

## 项目概述

**TableDiff** 是一个基于 Python + PySide6 的桌面端表格对比工具。用户可以导入两个 Excel/CSV 表格文件（初始版和修改版），工具会自动进行行匹配和逐列对比，以直观的颜色标记展示所有差异，并支持结果导出。

---

## 已实现功能清单

### P0（核心功能）
| 功能 | 状态 | 说明 |
|------|------|------|
| 导入 Excel/CSV 文件 | ✅ | 支持 .xlsx, .xls, .csv 格式，自动检测编码 |
| 双表自动对比 | ✅ | 基于键列的行匹配 + 逐列对比 |
| 差异状态标记 | ✅ | 新增(🟢)、删除(🔴)、修改(🟡)、相同(⚪) |
| 差异行高亮 | ✅ | 不同状态行有不同背景色 |
| 单元格级差异标记 | ✅ | 修改行中变化的单元格以橙色高亮 |
| 差异统计信息 | ✅ | 实时显示新增/删除/修改/相同行数 |
| 结果导出为 Excel | ✅ | 带颜色标记，含统计信息 sheet |

### P1（增强功能）
| 功能 | 状态 | 说明 |
|------|------|------|
| 选择键列 | ✅ | 下拉菜单自由选择匹配列 |
| 选择对比列 | ✅ | 复选框选择需要对比的列（支持全选/取消） |
| 仅显示差异行筛选 | ✅ | 一键切换，聚焦差异数据 |
| 差异导航 | ✅ | 上一条/下一条按钮，快速定位差异 |

### 🆕 新增高级功能（P2 → 已实现）
| 功能 | 状态 | 说明 |
|------|------|------|
| 🖱️ **拖拽导入** | ✅ | 直接把文件拖到窗口上加载 |
| 🔍 **列搜索** | ✅ | 输入关键词搜索定位特定对比列 |
| 📊 **汇总视图** | ✅ | 查看每列差异统计（差异数、差异率） |
| 🖱️ **差异详情弹窗** | ✅ | 双击某行弹窗，逐字段对比旧值→新值 |
| 📂 **文件历史** | ✅ | 记住最近打开的文件，一键重新加载 |
| 📑 **多 Sheet 支持** | ✅ | Excel 多 sheet 时弹出选择对话框 |

---

## 技术栈

| 层次 | 技术 | 用途 |
|------|------|------|
| 编程语言 | Python 3.10+ | 开发效率高，生态丰富 |
| GUI 框架 | PySide6 6.5+ | 控件丰富，表格组件强大，跨平台 |
| 数据处理 | pandas 2.0+ | 高性能向量化数据对比 |
| Excel 读写 | openpyxl 3.1+ | 支持样式导出的读写 |
| 打包工具 | PyInstaller（可选） | 可打包为独立 exe |

---

## 项目结构概览

```
table_diff_tool/
│
├── src/                          # 源代码
│   ├── main.py                   # 应用入口
│   ├── core/                     # 业务逻辑层
│   │   ├── loader.py             # 文件加载（多 Sheet 支持）
│   │   ├── comparator.py         # 表格对比算法
│   │   └── exporter.py           # 结果导出（Excel 带样式）
│   └── ui/                       # UI 表示层
│       ├── main_window.py        # 主窗口（组装 + 控制器）
│       ├── file_panel.py         # 文件面板（拖拽+历史）
│       ├── compare_panel.py      # 对比设置（列搜索）
│       ├── result_table.py       # 结果表格（双击详情）
│       ├── stats_bar.py          # 统计栏控件
│       ├── summary_dialog.py     # 汇总视图对话框
│       └── diff_detail_dialog.py # 差异详情弹窗
│
├── tests/                        # 测试
│   └── test_comparator.py        # 对比逻辑单元测试
│
├── design_doc.md                 # 阶段一：设计文档
├── architecture.md               # 阶段二：架构设计
├── project_plan.md               # 阶段三：项目计划
├── requirements.txt              # 依赖清单
├── README.md                     # 使用说明
└── SUMMARY.md                    # 本文件（项目总结）
```

---

## 如何运行

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 运行应用
python src/main.py
```

---

## 数据流

```
[Excel/CSV 文件]
      │
      ▼
FileLoader.load()         ── 检测编码、读取、解析（多 Sheet 选择）
      │
      ▼
  pandas DataFrame         ── 统一的表格数据结构
      │
      ▼
TableComparator.compare()  ── 按键列 outer join → 逐列对比
      │                         → 标记新增/删除/修改/相同
      ▼                         → 记录单元格级变化
  DiffResult                ── 包含合并表、统计数据、变化细节
      │
      └──→ ResultTable      ── 颜色高亮展示（双击查看详情）
      └──→ StatsBar         ── 统计数字
      └──→ SummaryDialog    ── 列汇总视图
      └──→ ResultExporter   ── 导出为带样式的 Excel
```

---

## 后续可扩展方向

1. **性能优化**：对于超大型文件（50万行+），实现分块读取和虚拟滚动
2. **更多格式支持**：支持 .json, .parquet, .db 等数据源
3. **自定义样式**：允许用户自定义差异标记颜色
4. **差异报告打印**：生成 HTML/PDF 格式的差异报告
5. **批量对比**：支持一次对比多对文件
6. **撤销/重做**：对比设置的撤销/重做操作
7. **国际化**：支持中英文切换
8. **命令行模式**：支持无界面批量对比（headless 模式）
9. **打包发布**：使用 PyInstaller 打包为单文件 exe

---

## 开发总结

本项目按照 **UI/UX 设计 → 架构设计 → 项目管理 → 编码实现** 四个阶段的完整流程进行交付：

| 阶段 | 交付物 | 主要内容 |
|------|--------|----------|
| 设计 | `design_doc.md` | 功能模块、界面布局、交互逻辑、视觉风格 |
| 架构 | `architecture.md` | 技术选型、分层架构、模块接口、数据模型 |
| 计划 | `project_plan.md` | WBS 任务分解、阶段划分、里程碑、风险评估 |
| 编码 | 完整项目代码 | 9个 Python 模块、测试用例、配置文件 |

新增了 **6 个高级功能**（拖拽导入、列搜索、汇总视图、差异详情弹窗、文件历史、多 Sheet 支持），全部通过验证测试。
