# 阶段一：桌面端应用设计文档

## 1. 需求分析

### 1.1 核心功能需求
| 编号 | 功能 | 描述 | 优先级 |
|------|------|------|--------|
| F1 | 导入初始版表格 | 支持导入 Excel (.xlsx/.xls) 和 CSV 格式文件作为初始版本 | P0 |
| F2 | 导入修改版表格 | 支持导入另一份 Excel 或 CSV 文件作为修改版本 | P0 |
| F3 | 自动对比差异 | 以行为单位，自动比对两个表格的数据差异 | P0 |
| F4 | 差异高亮展示 | 在表格中高亮显示有差异的行/单元格（增、删、改） | P0 |
| F5 | 筛选查看差异 | 只显示有差异的行，方便聚焦 | P1 |
| F6 | 导出对比结果 | 将对比结果导出为 Excel 文件，含差异标记 | P1 |
| F7 | 选择对比键列 | 用户可选择某列作为唯一标识键进行匹配对比 | P1 |
| F8 | 状态统计 | 显示新增行数、删除行数、修改行数的统计信息 | P0 |

### 1.2 目标用户
- 数据分析人员
- 运营人员
- 需要做数据版本对比的办公人员

### 1.3 使用场景
用户有两份结构相同（或相似）的表格数据（如产品列表、订单数据、员工信息等），分别为"初始版"和"修改版"。用户需要快速找出两表之间的差异，以便审核变更内容。

---

## 2. 信息架构

### 2.1 功能模块结构
```
桌面表格对比工具
├── 文件导入模块
│   ├── 导入初始版文件
│   ├── 导入修改版文件
│   └── 文件信息预览
├── 对比设置模块
│   ├── 选择键列（用于匹配行）
│   └── 选择要对比的列
├── 对比执行模块
│   └── 执行对比运算
├── 结果展示模块
│   ├── 完整对比表展示
│   ├── 差异筛选视图
│   ├── 差异统计面板
│   └── 差异导航（上/下一条差异）
└── 导出模块
    └── 导出对比结果
```

### 2.2 导航流程
```
[启动应用]
     │
     ├──→ [主窗口 - 初始状态（无文件）]
     │        │
     │        ├── 点击"导入初始版" → 文件选择对话框 → 加载显示
     │        └── 点击"导入修改版" → 文件选择对话框 → 加载显示
     │
     ├──→ [文件加载完成]
     │        │
     │        ├── 预览两个表格的前 N 行
     │        └── 选择键列和对比列
     │
     ├──→ [点击"开始对比"]
     │        │
     │        └──→ [结果展示视图]
     │               ├── 合并对比表格（含状态列）
     │               ├── 差异统计面板（新增/删除/修改数）
     │               ├── "仅显示差异" 切换开关
     │               └── "导出结果" 按钮
     │
     └──→ [导出] → 文件保存对话框 → 导出完成提示
```

---

## 3. 界面设计

### 3.1 主窗口布局

```
┌──────────────────────────────────────────────────────────────┐
│  📋 表格对比工具                                    _ □ ×  │
├──────────────────────────────────────────────────────────────┤
│  ┌──────────────────────┐  ┌──────────────────────┐         │
│  │ 📁 初始版             │  │ 📁 修改版            │         │
│  │ [选择文件...]  × 清除  │  │ [选择文件...]  × 清除 │         │
│  │ ────────────────────  │  │ ────────────────────  │         │
│  │ 文件名: 初始版.xlsx   │  │ 文件名: 修改版.xlsx   │         │
│  │ 行数: 1,234          │  │ 行数: 1,245          │         │
│  │ 列数: 15             │  │ 列数: 15             │         │
│  └──────────────────────┘  └──────────────────────┘         │
├──────────────────────────────────────────────────────────────┤
│  对比设置区域                                                 │
│  键列: [下拉选择列名 ▼]   对比列: ☑ 全选  □ 列A  ☑ 列B ...  │
├──────────────────────────────────────────────────────────────┤
│  [ ▶ 开始对比 ]     [ 📥 导出结果 ]   🟢 统计: +2 -5 ~12   │
├──────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────────┐ │
│  │  结果表格                                                 │ │
│  │  ┌─────┬──────┬────────┬────────┬────────┬──────────┐  │ │
│  │  │ 状态 │ 键值  │ 列A    │ 列B    │ 列C    │ ...      │  │ │
│  │  ├─────┼──────┼────────┼────────┼────────┼──────────┤  │ │
│  │  │ ➕新增│ 101  │ 数据.. │ 数据.. │ 数据.. │ ...      │  │ │
│  │  │ ➖删除│ 102  │ 数据.. │ 数据.. │ 数据.. │ ...      │  │ │
│  │  │ ✏️修改│ 103  │ 旧→新  │ 旧→新  │ 数据.. │ ...      │  │ │
│  │  │ ✅相同│ 104  │ 数据.. │ 数据.. │ 数据.. │ ...      │  │ │
│  │  └─────┴──────┴────────┴────────┴────────┴──────────┘  │ │
│  │  [☑ 仅显示差异行]  ◀ ▶ 差异导航                          │ │
│  └──────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

### 3.2 各界面详细说明

#### 3.2.1 文件导入区（顶部）
- **左右对称**：左侧为初始版，右侧为修改版，视觉一致，方便对比
- **选择文件按钮**：点击弹出系统文件选择对话框，支持 .xlsx, .xls, .csv
- **清除按钮**：移除已加载的文件
- **文件信息**：加载后显示文件名、行数、列数

#### 3.2.2 对比设置区
- **键列选择**：下拉列表，展示表格的所有列名，选择用于匹配行的主键列
- **对比列选择**：复选框列表，选择需要参与对比的列（默认全选）

#### 3.2.3 操作与统计栏
- **"开始对比"按钮**：主操作按钮，执行对比
- **"导出结果"按钮**：对比完成后可点击导出
- **统计信息**：实时显示 新增行数(+)、删除行数(-)、修改行数(~)

#### 3.2.4 结果展示区
- **状态列**：每行第一个单元格显示状态图标和文字
  - 🟢 `相同` — 行无变化（可用浅灰色背景）
  - 🟡 `修改` — 行数据有变化（可用浅黄色背景）
  - 🔴 `删除` — 在修改版中不存在的行（可用浅红色背景）
  - 🟢 `新增` — 在初始版中不存在的行（可用浅绿色背景）
- **差异单元格标记**：对于"修改"行，发生变化的单元格以橙色/粉色背景高亮，并显示"旧值→新值"的提示
- **筛选开关**："仅显示差异行"复选框，勾选后只显示状态为"新增""删除""修改"的行
- **差异导航**：上一条/下一条按钮，快速跳转到差异行

### 3.3 对话框设计

#### 文件选择对话框
- 使用系统原生文件对话框
- 文件类型过滤：Excel 文件 (*.xlsx *.xls), CSV 文件 (*.csv)

#### 导出对话框
- 使用系统原生保存对话框
- 默认文件名：`对比结果_初始版名_vs_修改版名.xlsx`

---

## 4. 交互逻辑说明

### 4.1 基本交互

| 操作 | 触发行为 |
|------|----------|
| 点击"选择文件" | 弹出文件选择对话框，选中文件后加载并显示预览信息 |
| 点击"清除" | 移除已加载文件，重置相关状态 |
| 选择键列 | 下拉选择后，自动更新后续对比逻辑的配置 |
| 勾选/取消对比列 | 实时更新参与对比的列集合 |
| 点击"开始对比" | 执行对比运算，完成后刷新结果表格和统计信息 |
| 点击"仅显示差异行" | 在结果表格中过滤显示，仅展示有差异的行 |
| 点击"差异导航"按钮 | 滚动到上一个/下一个差异行 |
| 点击"导出结果" | 弹出保存对话框，导出带差异标记的 Excel 文件 |

### 4.2 快捷键

| 快捷键 | 功能 |
|--------|------|
| Ctrl+O | 导入初始版文件 |
| Ctrl+Shift+O | 导入修改版文件 |
| Ctrl+R | 开始对比 |
| Ctrl+E | 导出结果 |
| Ctrl+N | 下一条差异 |
| Ctrl+P | 上一条差异 |

### 4.3 状态与反馈
- 文件加载中：显示加载动画或进度条提示
- 对比执行中：显示进度条和 "正在对比..." 文字
- 对比完成：状态栏显示 "对比完成，共 X 处差异"
- 导出完成：弹出提示框 "导出成功！"

---

## 5. 视觉风格建议

### 5.1 整体风格
- **风格定位**：现代简约风（Modern Flat）
- **灵感来源**：类似 Notion、Figma 的清爽界面
- **圆角**：控件使用 4-6px 小圆角

### 5.2 色彩方案

| 用途 | 色值 | 说明 |
|------|------|------|
| 主色 | #2563EB (蓝色) | 按钮、选中态、链接 |
| 背景色 | #F8FAFC | 主窗口背景 |
| 卡片背景 | #FFFFFF | 文件导入卡片、设置面板 |
| 分割线 | #E2E8F0 | 分隔区域 |
| 文字主色 | #1E293B | 主要文字 |
| 文字次要 | #64748B | 辅助文字 |
| 新增色 | #10B981 (绿色) | 新增行背景 #ECFDF5 |
| 删除色 | #EF4444 (红色) | 删除行背景 #FEF2F2 |
| 修改色 | #F59E0B (琥珀色) | 修改行背景 #FFFBEB |
| 差异高亮 | #F97316 (橙色) | 单元格差异背景 #FFF7ED |

### 5.3 字体选择
- 界面文字：系统默认字体（Segoe UI / Microsoft YaHei）
- 表格数据：等宽字体（Consolas / 微软雅黑）

### 5.4 控件样式
- 按钮：填充式主按钮（蓝色），次要按钮（线框样式）
- 输入框：浅灰色边框，聚焦时蓝色边框
- 表格：斑马条纹（奇偶行不同背景），有行悬停高亮
- 滚动条：自定义纤细滚动条

---

## 6. 假设说明
- 两个表格默认使用相同的列结构（列名一致），如果列结构不同，只对比共有的列
- 默认以第一列作为键列进行行匹配
- 表格文件默认以第一行为表头行
- 支持 GBK 和 UTF-8 编码的 CSV 文件
